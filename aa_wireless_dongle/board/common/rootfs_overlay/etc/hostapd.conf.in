ctrl_interface=/var/run/hostapd
interface=wlan0

hw_mode=g
ieee80211n=1
channel=6

auth_algs=1
wpa=2
wpa_key_mgmt=WPA-PSK
rsn_pairwise=CCMP

# ssid, wpa_passphrase and country_code are appended at boot by
# /etc/init.d/S39hostapd_conf from the values in /etc/aawgd.conf.
